## ADDED Requirements

### Requirement: Statistics placeholder tab
The Organizer screen SHALL include a "Stats" tab with two placeholder statistic cards displaying "AVG Weight" (mock: "2.4 kg") and "AVG Bag Opens" (mock: "4.2×/day") using the stacked-card design language.

#### Scenario: Stats values displayed
- **WHEN** the user opens the Stats tab
- **THEN** both AVG Weight and AVG Bag Opens cards are visible with placeholder values
